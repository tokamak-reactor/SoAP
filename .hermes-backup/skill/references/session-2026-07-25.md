# Session Notes — 25 July 2026

## Completed
- EIRENE branch: eirene_na (100% MATLAB match), tot_flux_th/r, tdena, P_neut
- Extractors: omp, imp, ft:N, wall (boundary=, x_axis_unit=), target_in/out
- extract_profile_ns() + find_flux_tube_by_r()
- Plot1D with multi-along, style presets
- Plot2D with PatchCollection (3 vertex paths: structured crn_r/crn_z, unstructured cv_vx, fallback)
- PlotConfig with include_guards, xlim, ylim
- SolpsWatch.compute_regions() and plot() shortcuts
- 9 commits from b8725d5 to 9ea7f60

## Decisions made
1. Regions are ALWAYS explicit (never auto-computed in extractors)
2. Every MATLAB function gets coverage check (extractor + plotter)
3. Plot2D uses PatchCollection, NOT tricontourf (avoids central hole)
4. `is_structured` flag is unreliable — check `imap_cv.ndim == 2`
5. Wall extractor rejects structured grids with clear error

## Skipped (logged in skipped-matlab-functions.md)
- All multi-case/multi-watch variants (Mode 2): 9 functions
- Will be covered later by ComparisonPlot

## Remaining
- calc_additional.m lines 171-1762
- Target/wall cells for structured grids
- PlotList YAML system
- ComparisonPlot (Mode 2), TimeSliderPlot (Mode 3)
- Interactive plotting
